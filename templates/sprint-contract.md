# Sprint Contract

Fill before activating a feature. Paste into the session or `PROGRESS.md`.

- Feature: `F__-___` (from `feature_list.json`)
- Scope: <1–3 sentences, files in / out>
- Non-goals: <explicitly excluded>
- DoD: `make verify-feature F=<id>` passes; layers L1→L3 in order; no debug artifacts
- Risks: <SSR, styling, content honesty>
- Evidence: commit hash + `verify-feature` output
